description = "Deployment command for production releases. Pre-flight checks and deployment execution."
prompt = """Execute o workflow abaixo:
@{.agent/workflows/deploy.md}

Argumentos do usuário: {{args}}
"""
